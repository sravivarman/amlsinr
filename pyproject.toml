[project]
name = "arkeshdas-portfolio"
version = "0.1.0"
description = "Arkesh Das's static personal portfolio website"
requires-python = ">=3.10"
dependencies = [
    "jinja2>=3.1",
    "markdown>=3.5",
    "pyyaml>=6.0",
]
