[project]
name = "sravivarman-conference"
version = "0.1.0"
description = "Sravivarman's static conference website"
requires-python = ">=3.10"
dependencies = ["jinja2>=3.1", "markdown>=3.5", "pyyaml>=6.0"]
